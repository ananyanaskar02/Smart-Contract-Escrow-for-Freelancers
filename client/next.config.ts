import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  allowedDevOrigins: ['svc-d7f8b73c-morphvm-hhzngv0z.http.cloud.morph.so'],/* config options here */
};

export default nextConfig;
